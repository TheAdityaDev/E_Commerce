import { createTheme } from "@mui/material/styles";

const customTheme = createTheme({
  platte: {
    mode: "Light",
    primary: {
      main: "#00927c",
    },
    secondary: {
      main: "#EAF0F1",
    },
  },
});


export default customTheme